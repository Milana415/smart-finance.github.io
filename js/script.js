function check() {
    let trueAnswer = 0, falseAnswer = 0
    let testResult="";
    let choise;
    for (let l=1; l<=5;l++) {
        let quetion = document.forms["test"].elements["question"+l];
        for (let i=1; l<=quetion.lenght; i++){
            if (l[i].checked) {
                choice==l[i].value
            }
            if (l[i].checked) {
                choice=l[i].value
            }
        }
    }
    alert()
}
    //test-radiobutton
$(document).ready(function () {
    $.each($('.'))
})